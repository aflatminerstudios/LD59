/*
with (other) {
	physics_apply_force(self.x, self.y, 20, -80)
}
*/

show_debug_message("x" + string(xSpeed) + "y" + string(ySpeed))
//physics_apply_impulse(x, y, xSpeed, ySpeed)

//scrMovePlatform(other.id)	